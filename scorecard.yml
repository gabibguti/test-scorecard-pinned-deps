annotations:
  - checks:
      - bla
      - pinned-dependencies
    reasons:
      # binary files / unpinned dependency files are only used for testing
      - reason: test-data
      - reason: remediated
